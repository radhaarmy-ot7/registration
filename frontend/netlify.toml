[build]
  command = "npm run build"
  publish = "dist"

# Handle React Router (prevents 404 on refresh)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Set Node version
[build.environment]
  NODE_VERSION = "18"